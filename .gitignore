node_modules/
build/
# CMake build trees (never commit generated deps)
build-cpp/
backend-cpp/build-cpp/
# Accidental nested clone tracked as gitlink without .gitmodules — keep local only
scripts/DEZHOU/
# Broken gitlink (no .gitmodules) — local nested clone only
tmp_wspp/
.vs/
*.exe
*.o
bin/

# Optional local WebSocket++ clone (see backend-cpp CMakeLists.txt)
backend-cpp/extern/
backend-cpp/cmake/__pycache__/
*.pyc

# Large local asset — not tracked (use CDN or manual copy on server)
assets/ifcan.mp3
